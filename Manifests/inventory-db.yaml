apiVersion: v1
kind: Service
metadata:
  name: inventory-db
spec:
  ports:
    - port: 5432
  selector:
    app: inventory-db
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: inventory-db
spec:
  serviceName: "inventory-db"
  replicas: 1
  selector:
    matchLabels:
      app: inventory-db
  template:
    metadata:
      labels:
        app: inventory-db
    spec:
      initContainers:
        - name: init-and-fix
          image: borsok/inventory-db:v1
          command:
            - "sh"
            - "-c"
            - "if [ ! -f /mnt/data/PG_VERSION ]; then cp -a /var/lib/postgresql/13/main/* /mnt/data/; fi; chown -R postgres:postgres /mnt/data && chmod 700 /mnt/data"
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: inventory-data
              mountPath: /mnt/data
      containers:
        - name: inventory-db
          image: borsok/inventory-db:v1
          ports:
            - containerPort: 5432
          env:
            - name: DB_NAME
              value: "movies_db"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: crud-secrets
                  key: INVENTORY_DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: crud-secrets
                  key: INVENTORY_DB_PASSWORD
          volumeMounts:
            - name: inventory-data
              mountPath: /var/lib/postgresql/13/main
  volumeClaimTemplates:
    - metadata:
        name: inventory-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
